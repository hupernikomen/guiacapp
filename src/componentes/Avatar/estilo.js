import { StyleSheet } from "react-native";

export default estilo = StyleSheet.create({
  container: {
    
    aspectRatio: 1,
    overflow: "hidden",
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#fff',
  }
})